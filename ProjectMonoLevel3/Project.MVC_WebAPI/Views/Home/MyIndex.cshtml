@model IEnumerable<Project.Model.DatabaseModels.VehicleMake>

@{
    ViewBag.Title = "MyIndex";
}

<h2>MyIndex</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VehicleMakeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Abrv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VehicleMakeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Abrv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VehicleMakeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.VehicleMakeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VehicleMakeId })
        </td>
    </tr>
}

</table>
